class Solution {
    public int arraySign(int[] nums) {
        boolean pos =true;
        for(int n : nums){
            if(n<0) pos = !pos;
            if(n==0) return 0;
        }
        return pos ? 1:-1;
    }
}
